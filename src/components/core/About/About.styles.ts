import { styled } from 'styled-components';

const AboutStyled = styled.div`
  .gh-section {
    border-top: none;
    margin-top: 4rem;
  }

  .linkedin-section {
    border-bottom: none;
    margin-bottom: 2rem;
  }
`;

export { AboutStyled };
